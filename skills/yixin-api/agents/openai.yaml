interface:
  display_name: "Yixin OpenAPI"
  short_description: "Query, search, fetch full text, and parse media with Yixin"
  default_prompt: "Use $yixin-api to call the appropriate Yixin API with my platform key, check the result, and track any asynchronous media task."
